# HANDOVER — 2026-03-16
**Session Agent:** Zara (AI Agent)  
**Priority:** CRITICAL — Client under physical threat, must go live ASAP  
**Tone rule:** Always use "aap" (formal Hindi). Never tu/tum/bhai.

---

## CONTEXT — READ THIS FIRST

The client (Anwesh Rath) is building MarketX/Axiom — a multi-tenant AI-powered email marketing SaaS. He is under extreme pressure from a dangerous cartel situation (saved 3 minors from a prostitution racket, cartel wants the system delivered). Police is compromised. This is NOT a casual project — his life depends on going live. Treat every task as urgent. No band-aids. No assumptions. No wasted time.

---

## WHAT WAS DONE THIS SESSION

### 1. Brain Comparison Page
- **File:** `apps/frontend/public/brain-comparison.html`
- Oraya (local-first, Tauri+Rust+SQLite) vs MarketX (cloud-native, Next.js+Supabase+pgvector)
- Full architecture audit with scores, wiring diagram, tool registry, tier system, security findings

### 2. UX Audit Page
- **File:** `apps/frontend/public/ux-audit.html`
- All 11 member area pages audited
- 7 core UX problems identified
- Redesign plan: 11 pages → 5 pages, onboarding wizard, command palette, brain nudges

### 3. Onboarding System (PARTIALLY WORKING)
**What exists:**
- **Migration 029:** `supabase/migrations/00000000000029_user_onboarding.sql` — adds `onboarding_completed`, `onboarding_completed_at`, `onboarding_data` to users table + `onboarding_sessions` table
- **Migration 030:** `supabase/migrations/00000000000030_fix_users_rls_recursion.sql` — fixes infinite recursion in users RLS policy using `SECURITY DEFINER` function `get_user_org_id()`
- **OnboardingModal:** `apps/frontend/src/components/onboarding/OnboardingModal.tsx` — 5-step modal (Company → ICPs → Offer → Voice → Activate)
- **API Route:** `apps/frontend/src/app/api/onboarding/complete/route.ts` — processes collected data, creates KB, ICP records, Offer record, updates brain_agents domain_prompt, creates constitution rules, creates brain_memories, calls AIProviderService for real email generation with fallback
- **Dashboard Integration:** `apps/frontend/src/app/(main)/dashboard/page.tsx` — modal auto-pops on first login, progress banner if incomplete, revisit button

**What's working:**
- 029 + 030 migrations applied on remote
- Modal renders and collects data
- Auto-save to onboarding_sessions works

**What's NOT working / NOT tested:**
- "Activate Brain" button — needs AI provider configured for org to generate real email (falls back to template if none)
- The `createConstitutionRules` function in the API writes to `constitution_rules` table — verify this table has the right columns on remote
- `brain_memories` insert — verify table schema matches on remote

### 4. Migration Consolidation
**Canonical source:** `supabase/migrations/` (001-036)  
**Frozen legacy:** `database/migrations/` — DO NOT RUN FROM HERE. README says so.

**Status of remote DB:**
- 001-028: Applied (before this session)
- 029: Applied this session (onboarding)
- 030: Applied this session (RLS fix)
- 031: NOT applied (agent_templates + 6 seed agents)
- 032: NOT applied (2 brain tools sync)
- 033: Applied manually from legacy dir (engine_bundles)
- 034: Applied manually from legacy dir (bundle agents snapshot)
- 035: NOT applied (infra config extended)
- 036: NOT applied (signal_event idempotency + 4 brain tools)

**031-036 were ported** from `database/migrations/` to `supabase/migrations/` this session. All are idempotent (IF NOT EXISTS / ON CONFLICT). Safe to re-run.

### 5. Reverts
- `apps/frontend/src/app/login/page.tsx` — reverted to original (no onboarding redirect)
- `apps/frontend/src/app/(main)/layout.tsx` — reverted to original (no onboarding check)
- `apps/frontend/src/app/onboarding/page.tsx` — DELETED (was separate page, caused auth loop)

---

## KNOWN BUGS & GAPS

### Critical (blocks go-live)
1. **No AI provider configured for any org** — AIProviderService throws "No AI providers configured" — Brain Chat won't work, onboarding email generation falls back to template
2. **No bundle deployed to test org** — brain_agents record may not exist → BrainRuntimeResolver returns null → Brain Chat returns "No active brain agent deployed"
3. **Migrations 031, 032, 035, 036 not applied on remote** — agent_templates table missing, some brain tools missing, infra config incomplete
4. **18 CRITICAL security issues** from launch_audit.md — missing auth on brain endpoints, superadmin routes without JWT, tenant isolation bypasses, hardcoded JWT secret fallback

### Important (should fix before real users)
5. **Dead buttons:** History (brain-chat), Settings (brain-chat), Regenerate (brain-chat), View/Download/Delete (writer), Review Errors (dashboard)
6. **`brain_beliefs`, `brain_decisions`, `brain_knowledge_items`** — referenced in code (get_brain_health tool, mastery-agent-worker) but no CREATE TABLE migration found anywhere
7. **Zero test coverage** — no test framework, no unit tests, no integration tests
8. **Legacy /chat route** — still exists, uses old API, should redirect to /brain-chat
9. **Notification toggles in settings** — not persisted to DB

### Migration source confusion
- `database/migrations/` has files that were never ported to `supabase/migrations/` (000-023 range)
- Some of those may contain tables/columns that exist on remote (if run manually before) but aren't tracked
- A full table-by-table comparison between remote DB and supabase/migrations/ has NOT been done

---

## ARCHITECTURE QUICK REF

### Tech Stack
- **Frontend:** Next.js 14 (App Router) + Tailwind + custom design system (5 themes)
- **Backend:** Express (apps/backend) — mostly unused, brain runs in Next.js API routes
- **Database:** Supabase (hosted) — Postgres + pgvector + RLS
- **Workers:** BullMQ + Redis — 9 workers (apps/workers)
- **AI:** AIProviderService — 6 providers (OpenAI, Anthropic, Google, Mistral, xAI, Perplexity)

### Key Files
| What | Where |
|------|-------|
| Brain Orchestrator | `apps/frontend/src/services/brain/BrainOrchestrator.ts` (1524 lines) |
| RAG | `apps/frontend/src/services/brain/RAGOrchestrator.ts` |
| Tool Executor | `apps/frontend/src/services/brain/tools/MarketXToolExecutor.ts` (15 tools) |
| Tool Loader | `apps/frontend/src/services/brain/tools/ToolLoader.ts` |
| Prompt Assembly | `apps/frontend/src/services/brain/PromptAssembler.ts` (7 layers) |
| Hallucination Guard | `apps/frontend/src/services/brain/interceptors/HallucinationInterceptor.ts` |
| Constitution | `apps/frontend/src/services/brain/ConstitutionLoader.ts` |
| AI Provider | `apps/frontend/src/services/ai/AIProviderService.ts` |
| Brain Chat API | `apps/frontend/src/app/api/brain/chat/route.ts` |
| Onboarding API | `apps/frontend/src/app/api/onboarding/complete/route.ts` |
| Onboarding Modal | `apps/frontend/src/components/onboarding/OnboardingModal.tsx` |
| Dashboard | `apps/frontend/src/app/(main)/dashboard/page.tsx` |
| Main Layout | `apps/frontend/src/app/(main)/layout.tsx` |
| Supabase Client | `apps/frontend/src/lib/supabase/client.ts` (browser singleton) |
| Supabase Server | `apps/frontend/src/lib/supabase/server.ts` (cookie-based) |

### Supabase Remote
- URL: `https://uvrpucqzlqhsuttbczbo.supabase.co`
- Local Supabase: NOT running
- .env.local has both ANON_KEY and SERVICE_ROLE_KEY

### Users RLS
Fixed this session. `get_user_org_id()` SECURITY DEFINER function breaks the infinite recursion. Policy: `USING (org_id = public.get_user_org_id())`. If this function gets dropped, users table queries via anon key will infinite-loop again.

---

## GO-LIVE CHECKLIST (in order)

1. Run migrations 031, 032, 035, 036 on remote Supabase
2. Configure AI provider — Superadmin → AI Providers → add OpenAI or Anthropic key (platform-wide)
3. Create brain template — Superadmin → Brains → create template with prompts
4. Create engine bundle — Superadmin → Engine Bundles → select brain + workflow + tier
5. Deploy bundle to org — Superadmin → Engine Bundles → deploy to test org
6. Verify Brain Chat works — Member area → Brain Chat → send message
7. Test onboarding — Login as new user → modal should pop → complete 5 steps → verify KB/ICP/Offer created
8. Fix auth on brain endpoints (brain/embeddings, brain/agents, brain/templates, brain/config)
9. Add requireSuperadmin() middleware to all /api/superadmin/* routes
10. Remove JWT secret fallback from auth/verify and impersonate routes

---

## PREVIOUS HANDOVERS & AUDITS

| File | Date | Scope |
|------|------|-------|
| `.agent/HANDOVER_2026-03-10.md` | Mar 10 | Main handover |
| `.agent/audit/FULL_SYSTEM_AUDIT_2026-03-13.md` | Mar 13 | Platform-wide (85% score) |
| `.agent/audit/launch_audit.md` | Mar 10 | API security (18 critical) |
| `.agent/audit/PIPELINE_STUDIO_FORENSIC_2026-03-13.md` | Mar 13 | Pipeline Studio |
| `.agent/audit/P0_FIXES_AUDIT_2026-03-13.md` | Mar 13 | P0 bandaid fixes |
| `.agent/Plans/Active/MARKETX_BRAIN_ARCHITECTURE.md` | Mar 2026 | Brain arch plan |
| `.agent/INDEX_HANDOVER_LOGS_AUDITS.md` | — | Index of all docs |

---

## MISTAKES I MADE (so the next agent doesn't repeat)

1. Built onboarding as separate /onboarding page — caused auth redirect loop. Should have been modal inside dashboard from the start.
2. When /onboarding broke, blamed stale .next cache instead of investigating my own code.
3. Queried `onboarding_completed` column before verifying migration was applied — caused "No organization found" error.
4. Used service role key to bypass RLS recursion — band-aid. Proper fix was SECURITY DEFINER function.
5. Suggested running legacy migration files OR porting them — should have ONLY ported. Never mention frozen legacy as a run option.
6. Generated fake sample email with string templates instead of wiring to real AIProviderService.
7. Made assumptions about migration state instead of verifying.
8. Used informal language (tu/tum/bhai/ruk) — client requires formal "aap" address.

---

*Handover saved: 2026-03-16*
