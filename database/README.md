# Database documentation

This folder holds architecture and setup documentation only.

**Canonical migrations** live in [`supabase/migrations/`](../supabase/migrations/).  
A legacy copy of the old `database/migrations/` content is in `_archive/migrations-system-b/` if needed for reference.
