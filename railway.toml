[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/workers/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# No healthcheckPath - workers are background processes, not web servers

[service]
name = "axiom-workers"
